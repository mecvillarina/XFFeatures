<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="XFFeatures.Views.MainPage"
             xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             Title="{Binding Title}">

    <StackLayout Padding="20">
        <Button Command="{Binding NavigateToCommand}"
                CommandParameter="CollectionViewPage"
                Text="Collection View (4.3)" />
        <Button Command="{Binding NavigateToCommand}"
                CommandParameter="RefreshViewPage"
                Text="Refresh View (4.3)" />
        <Button Command="{Binding NavigateToCommand}"
                CommandParameter="LabelPage"
                Text="Label (4.3)" />

        <Button Command="{Binding NavigateToCommand}"
                CommandParameter="NewFeaturesPage"
                Text="New Features (4.4 P)"  />
    </StackLayout>

</ContentPage>